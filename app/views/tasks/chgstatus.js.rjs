page.replace_html dom_id(@task), :partial => 'tasks/task', :object => @task
page.visual_effect :highlight, dom_id(@task), :duration => 1

page.replace "collaborations_sidebar", 
  :partial => 'shared/collaborations_sidebar'
  
unless @collaboration.nil?
  page.replace "topics_sidebar",
    :partial => 'shared/topics_sidebar'  
  page.replace "teams_sidebar",
    :partial => 'shared/teams_sidebar'
end
  
# TODO: only render those partials that are actually affected